openapi: 3.0.1
info:
  title: OpenAPI definition
  version: v0
servers:
  - url: http://localhost:8080/api
    description: Generated server url
paths:
  /repository/{id}/{case_id}:
    get:
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: case_id
          in: path
          required: true
          schema:
            type: string
      responses:
        200:
          description: OK
